apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: karpenter-nodepool
  namespace: argocd
spec:
  syncPolicy:
    preserveResourcesOnDeletion: true
  generators:
    - clusters: {}
  template:
    metadata:
      name: karpenter-nodepool
    spec:
      project: default
      source:
        repoURL: '{{metadata.annotations.karpenter_repo_url}}'
        path: '{{metadata.annotations.karpenter_repo_path}}'
        targetRevision: '{{metadata.annotations.karpenter_repo_revision}}'
        directory:
          recurse: false
      destination:
        namespace: default
        name: '{{name}}'
      syncPolicy:
        automated:
          allowEmpty: true
        syncOptions:
          - CreateNamespace=true
        retry:
          limit: 60
